# python-nostr
A Python implementation of the Nostr protocol, structured as a monorepo with three packages:   nostr_core — protocol primitives shared by the relay and client: event parsing, signature validation, filter matching nostr_relay — a WebSocket relay server nostr_client — a WebSocket client
